// מציג את כל המסדי נתונים במכונה
show dbs 

// מייצר מסד חדש או עובר למסד שקיים באותו שם 
 use <db_name>

// להוסיף רשומה/דוקמנט לקולקשיין יוזרז - אם יוזרז לא קיים יצור אותו ויוסף
// את הרשומה/דוקמנט אם כן קיים אז רק יוסיף
 db.users.insertOne({"name":"koko","age":40})


 // להוסיף מספר רשומות/דוקומנטים במכה אחת
  db.users.insertMany([{"name":"biden","age":79},{"name":"jack","age":25}])

// מחפש בקולקשיין/טבלה יוזרז את כל הרשומות
// SELECT * FROM users; כמו באס קיו אל
  db.users.find({})


; //  יחזיר רשומות שהמאפיין שם שלהם הוא ביידן
  db.users.find({name:"biden"})